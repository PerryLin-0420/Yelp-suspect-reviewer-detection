# Data outputs — not for repo
**result/
*.parquet
*.csv

# Analysis plots — selectively add with git add -f
K-means/analysis/
lifetime kmeans/analysis/
user behavier matrix/

# Local config — contains personal paths, never commit
config.py

# Internal
.claude/
__pycache__/
*.pyc
.ipynb_checkpoints/
